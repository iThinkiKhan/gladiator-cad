# Upper structure design log — retired

This file's content was merged into **`docs/GLADIATOR_DESIGN.md`** on 2026-09-17, which now covers
the whole vehicle (chassis, upper structure, mast, and a summary of the separate mast-head
workstream) in one place.

The full build history that used to live here — every revision, every measurement, every mistake
and fix in detail — is preserved in this file's git history (`git log -p -- docs/upper-structure.md`).
Nothing was deleted; it was consolidated so there weren't two separate, drifting design logs.
